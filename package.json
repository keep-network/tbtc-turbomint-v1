{
  "name": "tbtc-turbomint",
  "version": "1.0.0",
  "description": "",
  "main": "truffle-config.js",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "test": "truffle compile && npx mocha --exit --recursive test",
    "justtest": "npx mocha --exit --recursive test"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@openzeppelin/test-environment": "^0.1.2",
    "chai": "^4.2.0",
    "mocha": "^7.0.1"
  },
  "dependencies": {
    "@openzeppelin/test-helpers": "^0.5.4",
    "@openzeppelin/contracts": "^2.5.0"
  }
}
